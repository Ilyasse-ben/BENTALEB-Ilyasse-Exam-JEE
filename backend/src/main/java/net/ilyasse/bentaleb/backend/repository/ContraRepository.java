package net.ilyasse.bentaleb.backend.repository;

import net.ilyasse.bentaleb.backend.entity.Contra;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContraRepository extends JpaRepository<Contra,Long> {
}
